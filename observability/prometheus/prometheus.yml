global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: checkout-service
    metrics_path: /metrics
    static_configs:
      - targets:
          - checkout-service:3000
