apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: sre-lab
data:
  prometheus.yml: |
    global:
      scrape_interval: 5s

    scrape_configs:
      - job_name: checkout-service
        metrics_path: /metrics
        static_configs:
          - targets:
              - checkout-service:3000
